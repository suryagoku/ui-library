{
  "extends": "../../tsconfig.base.json",

  "compilerOptions": {
    "rootDir": "./src",

    // tsdown produces dist/ (JS, .d.mts and sourcemaps); tsc is only ever run to typecheck,
    // so it must not write anything next to the sources.
    "noEmit": true
  },

  "include": ["src"]
}
